{
  "name": "tanksalot-web",
  "version": "1.0.0",
  "private": true,
  "description": "Image optimization tooling for the tanksalot marketing site",
  "type": "module",
  "scripts": {
    "build": "bundle install && bundle exec jekyll build --config _config.yml,_config.prod.yml",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev",
    "optimize": "node bin/optimize-images.mjs"
  },
  "devDependencies": {
    "sharp": "^0.34.0",
    "wrangler": "^4"
  }
}
